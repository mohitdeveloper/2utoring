@model IEnumerable<StandingOut.Data.Models.User>

@{
    ViewData["Title"] = "Admin Users";
}



<div class="grid-container grid-container--wide">
    <div class="grid-x margin-bottom-loose margin-top-extra-loose">
        <div class="large-6 cell">
            <h2>@ViewData["Title"]</h2>
        </div>
        <div class="large-6 cell text-align-right display--flex flex--spaceBetween flex--alignCenter">
            <a class="button button--primary button--hasIcon margin-left-auto" asp-action="Create"><i class="fal fa-plus"></i><span>Add User</span></a>
        </div>
    </div>
    <div class="grid-x">
        <div class="large-12 cell">
            <section class="table margin-top">
                <table class="datatable" cellpadding="0" cellspacing="0">
                    <thead>
                        <tr>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Email</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.FirstName)</td>
                            <td>@Html.DisplayFor(modelItem => item.LastName)</td>
                            <td>@Html.DisplayFor(modelItem => item.Email)</td>
                            <td align="right">
                                <div class="display--flex flex--spaceBetween">
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="button button--primary margin-left-auto">Edit</a>
                                    <a asp-action="Delete" asp-route-id="@item.Id" class="button button--distructive margin-left-tight">Delete</a>
                                </div>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </section>
        </div>
    </div>
</div>
